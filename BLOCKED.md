# BLOCKED — what I need from Evan

- [ ] 🔴 **Domain-migration decisions (DOMAIN-MIGRATION.md Phase 0)** — confirm three items before Phase 1 (DNS move): (1) the subdomain map (proposed in the table — adjust if needed), (2) Guzzolene — keep a separate domain or add `guzzolene.evanappel.me`?, (3) the Vercel project "web" — what is it and does it need a subdomain? These determine the Cloudflare DNS records. Target completion by early November 2026, before the December Wix renewal.
- [ ] 🟡 **Phase-0 DNS inventory** — screenshot/export all DNS records from Wix as a safety net (~30 min, zero risk) and note the registration's 60-day transfer-lock eligibility date. Recommended before Phase 1.
- [ ] 🟡 **Screenshots to promote wordly + weather into the showcase carousel** — both now ship as `featured` "More projects" entries. To lift either into the curated 6-project showcase, the tests require a real screenshot at `public/assets/screenshots/<id>.png` (e.g. `wordly.png`, `weather.png`). I cannot capture these: browser automation is off by guardrail, and wordly's board is behind magic-link auth so it is unreachable headlessly. **To unblock:** take a PNG of each running app (wordly: a logged-in game board; weather: the forecast + radar view), drop it in that folder, and tell me — I will add the `screenshot` field, assign a `showcase` rank, and update the hardcoded showcase test.
- [ ] 🟡 **LinkedIn URLs + publish dates for the /writing essays** — the 10 essays now live under `/writing` but the "Originally published on LinkedIn" backlink, the date line, and the canonical SEO tag stay hidden until the real values are supplied. For each essay, fill `linkedInUrl` and `publishedAt` in `data/writing.ts` (match by `slug`; date is ISO, e.g. `"2025-07-16"`). These are real facts only you have — don't let me invent them. Pages render fine without them; this just completes the LinkedIn attribution.
